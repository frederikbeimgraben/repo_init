"""
Default Repo Initializer

Builds a default project configuration including a README, a LICENSE,
a default setup.py, a default requirements.txt and a default .gitignore.

If requested, the project will also be initialized with a DESIGN conventions
file, a ENVIRONMENT documentation file, a corresponding environment configuration
and create subfolders for the project.
"""

from model import *
import repo_init